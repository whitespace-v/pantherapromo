services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pantherapromo
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    # env_file:
    #   - .env
